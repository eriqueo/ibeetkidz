{
  "name": "ibeetkidz",
  "private": true,
  "license": "UNLICENSED",
  "version": "0.0.0",
  "type": "module",
  "description": "Kidpix for audio — a kid-friendly browser sound playground. Record your voice, make it crazy, layer loops, see the sound.",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:notices && tsc && vite build && npm run build:gh && npm run check:no-editor && npm run check:pwa",
    "build:gh": "vite build --mode gh",
    "check:no-editor": "bash scripts/check-no-editor-in-dist.sh",
    "check:notices": "node scripts/generate-third-party-notices.mjs --check",
    "check:pwa": "node scripts/check-pwa-build.mjs",
    "check:workshop-car-art": "python3 scripts/validate_workshop_open_car_art.py",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:unit": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "npm run build:gh && playwright test",
    "test:e2e:headed": "npm run build:gh && playwright test --headed",
    "deploy": "npm run build && npm run deploy:cf",
    "deploy:cf": "npx -y wrangler@4 pages deploy ./dist --project-name=ibeetkidz --branch=main --commit-dirty=true",
    "generate:notices": "node scripts/generate-third-party-notices.mjs"
  },
  "dependencies": {
    "phaser": "^4.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tone": "^15.0.4",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.56.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "@vitest/coverage-v8": "^3.2.3",
    "eslint": "^10.8.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "fake-indexeddb": "^6.2.5",
    "globals": "^17.8.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.65.0",
    "vite": "^6.4.1",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^3.2.3"
  }
}
